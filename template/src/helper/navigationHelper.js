// import {useNavigation} from '@react-navigation/native';

// const reset = (key, options) => {
//   const navigation = useNavigation();
//   navigation.reset(key, options);
// };

// const push = (key, options) => {
//   const navigation = useNavigation();
//   navigation.navigate(key, options);
// };

// const replace = (key, options) => {
//   const navigation = useNavigation();
//   navigation.navigate(key, options);
// };

// export default {}
